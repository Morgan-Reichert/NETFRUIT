# Dependencies
node_modules/
app/node_modules/

# Build output
dist/
app/dist/

# Local secrets — never commit
.env
app/.env
*.local

# Generated episodes ARE committed so they deploy to Vercel (kept small: a few
# short clips per series). If this grows large, move media to Supabase Storage.

# OS / editor
.DS_Store
*.log

# Scratch
scratchpad/
